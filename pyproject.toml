# Tooling for the in-tree Python drivers.

[tool.ruff]
target-version = "py314"
line-length = 109
# Exclude build outputs and the verbatim vendored sandbox.
extend-exclude = ["buck-out", "vendor/mkosi/sandbox.py"]
src = ["archive", "cargo", "engine", "image", "image_format", "package_system/rpm", "rootfs", "vendor"]
lint.select = [
    "E",    # pycodestyle
    "W",    # pycodestyle (trailing whitespace)
    "F",    # pyflakes
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "ANN",  # flake8-annotations
]

[tool.ruff.lint.per-file-ignores]
# Keep the vendored implementation byte-for-byte identical after its provenance header.
"vendor/mkosi/versioncomp.py" = ["ANN204"]

[tool.ty.environment]
python-version = "3.14"
# Runtime trees use a flat import namespace; third-party imports come from `--python`.
extra-paths = ["archive", "cargo", "engine", "image", "image_format", "package_system/rpm", "rootfs", "tine", "vendor"]

[tool.ty.src]
# Verbatim vendored modules do not follow Tine's project-specific type rules.
exclude = ["buck-out", "vendor/mkosi/sandbox.py", "vendor/mkosi/versioncomp.py"]

[tool.ty.rules]
all = "error"
