"""Development-box smoke target."""

load("@tine//box:build.bzl", "box")

box(
    name = "demo.box",
    packages = [
        "bash",
        "coreutils",
        "fish",
    ],
    release = "tine//catalog:fedora.rawhide.release",
)
