# Refuse any extension whose verity root hash is not signed by a certificate in
# /usr/lib/verity.d: `signed` grants no unsigned or unverified-signature fallback.
[SysExt]
ImagePolicy=root=signed+absent:usr=signed+absent:=ignore
