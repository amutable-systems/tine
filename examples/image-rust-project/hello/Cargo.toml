[package]
name = "hello"
version = "0.1.0"
edition = "2024"

# Deliberately not the package name, so a `:hello[hello-cli]` reference is easy to read.
[[bin]]
name = "hello-cli"
path = "src/main.rs"

# Real dependencies with transitive ones of their own, so the vendored tree and the SBOM have
# something to show.
[dependencies]
anyhow = "1"
serde_json = "1"
