#!/bin/sh
set -eu

caller=$PWD
cell=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$cell"
exec "$cell/tools/buck" -v 0 run --console none tine//tine:tine -- \
    --buck "$cell/tools/buck" \
    --source-root "$cell" \
    --caller-directory "$caller" \
    "$@"
