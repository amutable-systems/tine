# buck output / state
/buck-out
**/buck-out

# Local (non-committed) config overrides
/.buckconfig.local
.claude/settings.local.json

# Build output of a cargo build run by hand inside a checked-out Rust project
target/

# Python tooling caches (ruff/ty pinned in tools/)
.ruff_cache/
.ty_cache/
__pycache__/
*.pyc

# Editor / OS noise
*.swp
.DS_Store
