"""The active catalog: repositories, OS releases, engines, package managers, and build profiles."""

# Repoint the `catalog` cell to replace these root-package declarations. Generated data lives under
# `snapshot/{repo,engine}/`, without target-type suffixes in filenames. A new engine transaction may start
# as `{}` when `resolver_engine` names a working predecessor. Refresh after adding either kind of target.
load("@tine//engine:rules.bzl", "engine")
load("@tine//package_system/rpm:catalog.bzl", "centos_stream_release", "fedora_release")

fedora_release(
    name = "fedora.44",
    version = "44",
    engine = ":fedora.rawhide.engine",
    visibility = ["PUBLIC"],
)

# Pinned rpmrepo compose snapshot: packages never vanish underneath committed pins.
# refresh-catalog enumerates the mirror and advances the snapshot pin to the newest compose.
fedora_release(
    name = "fedora.rawhide",
    version = "rawhide",
    engine = ":fedora.rawhide.engine",
    rpmrepo_mirror = "https://rpmrepo-gateway.amutable.workers.dev/v2/mirror/public/f45",
    rpmrepo_snapshot = "f45-x86_64-rawhide-20260715",
    visibility = ["PUBLIC"],
)

centos_stream_release(
    name = "centos.10-stream",
    version = "10-stream",
    engine = ":fedora.rawhide.engine",
    visibility = ["PUBLIC"],
)

# Shared RPM engine plus the image assembly and VM tools. These tools stay out of images.
engine(
    name = "fedora.rawhide.engine",
    packages = [
        "rpm",
        "python3",
        "python3-libdnf5",
        "python3-createrepo_c",
        "coreutils",
        "libseccomp",
        "ca-certificates",
        "systemd-udev",
        "systemd-ukify",
        "python3-pefile",
        "e2fsprogs",
        "erofs-utils",
        "veritysetup",
        "dosfstools",
        "mtools",
        "systemd-container",
        "qemu-system-x86-core",
        "edk2-ovmf",
        "openssh",
    ],
    release = ":fedora.rawhide.release",
    visibility = ["PUBLIC"],
)
